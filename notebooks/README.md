# Notebooks

Use notebooks for exploration. Move reusable logic into `src/citadel_comp/`. Clear outputs before committing notebooks; save generated datasets, models, and figures in the ignored artifact directories.
